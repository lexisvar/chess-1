/*
TODO if these methods are gonna be this big they need to be in their own static (Colours)
*/

/*
	colour_name: function(colour) {
		var fen={w: "white", b: "black"};
		var name={white: "white", black: "white"};
		var num=[];

		num[WHITE]="white"
		num[BLACK]="black";

		if(colour in fen) {
			return fen[colour];
		}

		else if(colour in name) {
			return name[colour];
		}

		else {
			return num[colour];
		}
	},

	colour_fen: function(colour) {
		var fen={w: FEN_ACTIVE_WHITE, b: FEN_ACTIVE_BLACK};
		var name={white: FEN_ACTIVE_WHITE, black: FEN_ACTIVE_BLACK};
		var num=[];

		num[WHITE]=FEN_ACTIVE_WHITE;
		num[BLACK]=FEN_ACTIVE_BLACK;

		if(colour in fen) {
			return fen[colour];
		}

		else if(colour in name) {
			return name[colour];
		}

		else {
			return num[colour];
		}
	},

	colour_int: function(colour) {
		var fen={w: WHITE, b: BLACK};
		var name={white: WHITE, black: BLACK};
		var num=[];

		num[WHITE]=WHITE;
		num[BLACK]=BLACK;

		if(colour in fen) {
			return fen[colour];
		}

		else if(colour in name) {
			return name[colour];
		}

		else {
			return num[colour];
		}
	},
*/